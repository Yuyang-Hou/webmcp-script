# 安全边界与报告

当前维护最新预览版本，仓库保持私有。脚本运行在页面 MAIN world，不能隔离恶意页面或脚本；只安装可信代码。工具可见不代表操作已获授权。配对凭据只用于本机 relay，不能作为多用户访问控制。

在私有仓库中提交脱敏的复现步骤与影响版本；需要凭据或个人信息才能复现时，先联系维护者确定受限传递方式。不要在 Issue 或日志中粘贴配对码、站点身份凭据或个人信息。不承诺固定响应时间。
