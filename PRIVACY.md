# 隐私与数据流

适用版本：0.4.0-beta.3。扩展和桥接没有内置分析埋点、广告或项目云端账户服务。

## 哪些数据在哪里

- 扩展在 Chrome 本地存储保存脚本源码、上一版本、启用状态及连接设置。脚本按匹配范围在页面 MAIN world 执行，可访问该页面的数据。
- 目录功能还可保存用户确认的项目入口名称和 URL；调用 browser_catalog 会向 AI 返回入口及已安装脚本的名称、说明、版本、范围和启停状态，不返回源码或连接密钥。入口可用 browser_entry 的 remove 删除，卸载扩展也会清除；不要保存含令牌的网址。
- 本机桥接默认仅监听 `127.0.0.1:17891`。配对码、诊断日志和可选 CLI 脚本库保存在项目 `.local/`，不进入发布包。
- AI 调用页面发现时，可以获得已打开的 HTTP(S) 标签页标题、URL、工具名称、描述、schema 和调用结果。网页工具描述与返回值可能包含个人或业务数据。
- 这些数据通过本机 MCP 进入你选择的 AI 客户端。该客户端可能把它们发送给远程模型；其保存、训练和删除政策由对应服务决定。本项目不能保证数据始终不离开电脑。
- 安装的第三方脚本及网站自身可能向外发送数据；本项目不能约束它们的网络行为。只安装可信来源的源码。

## 为什么需要这些权限

`tabs` 用于列出及定位页面；`scripting` / `userScripts` 用于注册和执行经用户安装的脚本与页面桥接；`storage` 保存本地设置；`webNavigation` 跟踪文档变化；`alarms` 用于连接维护。HTTP(S) 网站访问权限支持不同站点的工具发现，单个脚本执行仍受其 `@match` 限制。

没有跨源 iframe 工具汇总。脚本运行不是安全沙箱；页面及工具返回的指令不自动构成用户授权。

## 删除与反馈

删除脚本不撤回已经发送给网站或 AI 的数据，也不能撤销既有业务操作。卸载扩展清除其本地存储；本地桥接文件需自行删除，操作前请备份需要保留的脚本。问题反馈中不要粘贴配对码、cookie、完整页面数据或个人信息。

## 用户配置的脚本更新

用户手动检查或启用自动检查后，扩展会请求绑定的 HTTPS 更新/下载地址，获取脚本元数据和源码。请求不携带 Cookie 或页面来源，不上传已安装源码；发布源仍能看到网络请求与 IP。更新策略、版本、检查时间及上一版源码仅保存在扩展本地。仅检查模式不安装，自动安装需用户选择并信任发布源；不要求 MCP 或 AI 在线。不提供托管脚本服务。
